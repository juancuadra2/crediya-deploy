# =====================
# Archivos y carpetas a ignorar
# =====================

# Gradle cache y builds
.gradle
**/build
build
**/build-cache

# Git
.git
.gitignore

# IDEs / configuración local
.idea
.vscode
*.iml

# Logs y archivos temporales
*.log
*.tmp
*.swp
*.DS_Store

# Node / front (si hay integraciones)
node_modules
dist
coverage

# Maven (por si hay mezcla)
target

# Archivos de Docker
Dockerfile
**/Dockerfile
docker-compose*.yml

# =====================
# IMPORTANTE: no ignores gradlew
# =====================
# No excluir gradlew ni gradlew.bat, son necesarios para builds reproducibles
# Por eso NO se pone `*.bat` ni `*.cmd` aquí
